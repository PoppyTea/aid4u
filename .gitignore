# Claude Code — lokalne nadpisania (nie commituj)
.claude/settings.local.json

__pycache__/
*.py[cod]
*.egg-info/
.venv/
dist/

# Środowisko
.env
.env.vps
.env*
.env.*
.zed/*

# Foldery pomocnicze
.help/
.tw_task/
agents/
4th-devs/
/**/.tw_task/**/*


# Cache danych z hubu (nie commituj — każdy ma swój apikey)
.cache/

# Stan synchronizacji rejestru promptów (SHA-256 per prompt → wersja w Langfuse).
# Lokalny cache przyspieszający sync_prompt(), nie źródło prawdy — patrz
# core/observability/prompts.py + strategy/observability.md.
.langfuse-prompt-state.json

# Kill switch — PGID i wartownik STOP bieżącego przebiegu (efemeryczne, per-maszyna).
# Patrz core/runtime/killswitch.py + core/AGENTS.md.
.run/

# Historia odpowiedzi agenta per-run (nie commituj — lokalny audit trail).
# Nie mylić z data/output/ (bez "s") — tam trafiają dane faktycznie przydatne
# w kolejnych zadaniach, commitowane. Patrz data/AGENTS.md.
data/run-history/

# Flagi (opcjonalnie — możesz commitować żeby śledzić postępy)
# .flags.json


# IDE
.idea/
.vscode/
*.swp

# Coverage
.coverage
html[cov]/

# uv
uv.lock
.python-version

# filetypes
*.txt
*.log

# github PR workflow
#    pull request summaries etc
**/pr-*.md
**/pr_*.md
**/*-pr-*.md
**/PR-*.md
**/*-PR-*.md
**/PR_*.md
tasks/s01e02_findhim/tmp-quick-scripting-tests.py

# Root-level scratch docs only — unanchored `doc/` was matching every
# tasks/*/doc/ folder repo-wide and silently blocking them from ever being
# committed (found 2026-08-05: s01e03/s01e04/s01e05/s02e01/s02e03 doc/ were
# all untracked despite tasks/AGENTS.md documenting doc/ as part of the
# intended per-episode structure).
/doc/
keyring-index.md
# Stan rutyn audytowych — lokalny cache, źródłem prawdy findingów jest Linear
# (patrz strategy/quality-control.md). Katalogiem, nie per plik: nowa rutyna = nowy
# plik stanu, który ma być ignorowany od razu, bez edycji tego pliku.
.claude/state/
.ruff_cache/
.pytest_cache/
.zed/
