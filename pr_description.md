🎯 **What:** Tested the structured output and markdown stripping logic in `AnthropicAdapter.complete_structured`.
📊 **Coverage:** Covered plain JSON, markdown-wrapped JSON, system prompt merging, and schema validation errors.
✨ **Result:** Increased test coverage and confidence in `complete_structured` behavior.
