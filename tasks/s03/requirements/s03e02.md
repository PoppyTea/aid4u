# s03e02 — firmware

**Status:** ✅ **zaliczone 2026-08-20** — `{FLG:CANTTOUCHTHIS}`, zero LLM, koszt $0.00,
domyka Sezon 3 (5/5). Co faktycznie zadziałało: `tasks/s03e02_firmware/AGENTS.md`.

**Zadanie:** zdalny, okrojony Linux shell (`POST /api/shell`) na maszynie z prawami
zwykłego użytkownika. Uruchom `/opt/firmware/cooler/cooler.bin`, znajdź hasło,
popraw `settings.ini`, zdobądź kod `ECCS-...`.

⚠️ **Najdroższe i najbardziej karzące zadanie sezonu wg społeczności — rozrzut kosztu
×140 między podejściami ($0.05 vs $7.20 za to samo zadanie).** Nie wchodzić bez
poniższych osłon.

## Zanim zaczniesz — TWARDE wymagania (nie opcjonalne)

> **Stan tych wymagań żyje w Linear, nie w tym pliku.** Przed startem odpytaj label
> `pre-e02` (patrz `strategy/issue-tracking.md`). Ten dokument mówi **czego** wymagać
> i **dlaczego** — nie czy jest już spełnione. Checkbox tutaj zestarzałby się przy
> pierwszej zmianie w `core/`, a czytany później wprowadzałby w błąd.

**Limit rozmiaru wyniku narzędzia.** `cat` na dużym pliku binarnym kosztowało
uczestnikom $5-10 pojedynczym wywołaniem. Wymóg: obcięcie ZANIM wynik trafi do
kontekstu, nie tylko w logu (`truncate_tool_result()`, patrz `core/AGENTS.md`).

**Rate limiter wychodzący na `/api/shell`.** Limit ~30 req/min;
429/`{"rate_limited": true}` kumuluje się przy zbyt wczesnych retry. Throttle musi być
wymuszony w kodzie, nie zależny od tego, czy model „zdecyduje się" czekać. (→ AID-46)

**Blacklist ścieżek** (`/etc`, `/root`, `/proc`, wszystkie `.gitignore` po drodze)
**wymuszona w kodzie narzędzia, NIE w promptcie.** Staff explicite błogosławi hardcode
tutaj — złamanie reguł = ban + reset VM. (→ AID-47)

**Propagacja błędów narzędzia do modelu.** Model musi widzieć KONKRETNY sygnał (ban,
rate limit, 503), żeby zareagować, nie zgadywać. (→ AID-48)

## Wybór modelu — konkrety, nie ogólniki

Rozrzut w komentarzach jest ekstremalny i sprzeczny (ten sam model bywa "zwycięzcą" i
"katastrofą" u różnych osób) — traktuj to jako eksperyment, nie pewnik.

- Sonnet 4.6: bywa pierwszorazowe, ale **realne przypadki $3-7.20 za nieudaną
  próbę** w nieosłoniętej pętli.
- Tańsze modele (gemini-3-flash/-flash-lite, gpt-5-mini, gpt-5.2/5.4,
  mistral-small-3.1) rozwiązywały to samo zadanie za $0.05-0.12.
- **Zalecenie operacyjne:** zaczynaj tanio, twardy limit iteracji (~30-50),
  eskaluj TYLKO ten pod-krok który faktycznie zawodzi — nie cały model od razu.

## Gotchas potwierdzone przez społeczność

- Komunikat serwera `"SAFETY_CHECK is not set in settings.ini"` bywa mylący — linia
  MOŻE tam być, tylko zakomentowana/ze złą wartością.
- Sekrety są **case-sensitive**.
- Shell odrzuca łańcuchy poleceń (`a && b`), `cd`, `./cooler.bin` — to ograniczony
  zestaw komend, nie prawdziwy bash. Model po ~10 iteracjach zaczyna "wymyślać"
  składnię.
- Po banie/`reboot` trzeba odtworzyć CAŁĄ konfigurację w kolejności — nic nie
  przetrwa resetu.
- Historia shella to zamierzona ścieżka podpowiedzi (pokazuje jak binarka była
  wywoływana z argumentami).

Pełny kontekst: `source/tool-inventory.md` (sekcja 2.1), `source/community-intel.md`
(sekcja s03e02 — najdłuższa sekcja w raporcie, warto przeczytać w całości przed
startem).


## Co faktycznie zadziałało (2026-08-20)

**Bez pętli agentowej.** Sonda `help` pokazała 13 komend i to, że przestrzeń problemu
jest mała: trzy linie w `settings.ini`, jeden plik blokady, jedno hasło. Pętla dokładałaby
koszt i niedeterminizm, żeby model odkrył znane — przy udokumentowanym rozrzucie ×140 to
zły interes. Osłony pracowały tak samo w trybie deterministycznym.

**Osłony zadziałały w praktyce, nie tylko w testach:**
- Bramka odrzuciła `rm`, `reboot`, `.env`, `storage.cfg`, `logs/` i `/etc/passwd` —
  **żadne nie dotarło do huba**, każde skończyło się wpisem w logu.
- `format_tool_error` przy `cooler.bin` bez argumentu podało kod 400, treść serwera
  (`-872 Usage:`) i wskazówkę „błąd trwały, popraw argumenty".
- `find` bez trafień zwraca 404; narzędzie zamieniło to na czytelny tekst zamiast
  wywalić przebieg.

**Pułapki, których intel nie opisuje:**
- `.env` jest w `.gitignore` — czyli oczywiste miejsce na hasło jest drogą do bana.
  Hasło (`admin1`) leży w `/home/operator/notes/pass.txt`.
- `cooler.bin admin1` nie zwraca błędu hasła, tylko `Lock file exists…`. Łatwo uznać,
  że hasło jest złe, i zacząć zgadywać.
- Binarkę uruchamia się **podając ścieżkę jako polecenie**, nie przez `run`/`exec`.
