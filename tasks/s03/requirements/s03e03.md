# s03e03 — reactor

**Status: rozwiązane (2026-08-17)** — flaga `{FLG:INSTALLED}`, `tasks/s03e03_reactor/`.
9 ruchów, 0 zgnieceń, koszt $0.00. Poszliśmy wprost ścieżką "bez LLM" zalecaną
poniżej — deterministyczny receding-horizon BFS (obserwuj stan z API → BFS pełnego
planu → weź pierwszy ruch → guardrail programistyczny → wyślij → powtórz).

**Co faktycznie zadziałało:**
- Lekcja S03E03 **nie podaje żadnego formatu JSON** dla `/verify` — potwierdzone
  dwukrotnym zapytaniem do NotebookLM. Format `answer: {"command": "..."}` (nie goły
  string!) i pełny kształt odpowiedzi (`board`/`player`/`goal`/`blocks`/`reached_goal`)
  ustalone empirycznie sondą (`tasks/s03e03_reactor/scripts/probe_api.py`) PRZED
  napisaniem solvera — bez tego solver byłby napisany pod zgadywaną strukturę.
- **Kolizja sprawdzana PO przesunięciu bloków, nie przed** — nieoczywiste i
  zmierzone eksperymentalnie w obie strony (ruch w kolumnę bezpieczną-teraz-ale-
  zajętą-po-ticku = zgniecenie; ruch w kolumnę zajętą-teraz-ale-pustą-po-ticku =
  sukces). To zmienia, które kolumny są bezpieczne względem "wyświetlanego" stanu.
- `blocks` w odpowiedzi API niesie jawne pole `direction` per blok — nie trzeba było
  wyprowadzać fazy cyklu z historii, tylko z bieżącej odpowiedzi.
- Pełny opis mechanizmu, testy (w tym regresja wobec prawdziwych fixtures z sondy)
  i kontrakt zadania: `tasks/s03e03_reactor/AGENTS.md`.

---

## Treść zadania (przed podejściem — zachowane jako ślad)

**Zadanie:** poprowadź robota przez planszę 7×5, omijając ruchome bloki reaktora
(cyklicznie góra/dół, poruszają się TYLKO gdy wydajesz polecenie). Komendy: `start`,
`reset`, `left`, `right`, `wait`.

**Najłatwiejsze zadanie sezonu wg społeczności** — traktuj jako rozgrzewkę, nie jako
poligon do przesadnej architektury.

## Zanim zaczniesz

- [ ] Rozważ zrobienie tego BEZ LLM w ogóle — API zwraca pełny stan planszy, więc to
  klasyczny problem CS (symulacja + planowanie), nie zadanie językowe. Kilku
  uczestników zrobiło to czystym BFS w 8-12 ruchach.
- [ ] Jeśli jednak z modelem: nie wysyłaj surowego stanu API — LLM-y są słabe w
  rozumowaniu przestrzennym 2D i "wchodzą" w bloki. Pre-digest planszy (tabela
  markdown / jawne "ruch w lewo/prawo teraz niebezpieczny") działa lepiej.
- [ ] **Programistyczny override jest jawnie błogosławiony przez staff:** jeśli model
  mówi `right` a pole po prawej zajęte — wymuś `wait` w kodzie, nie ufaj ślepo
  decyzji modelu. To hybryda, nie oszustwo.
- [ ] Feeduj modelowi tylko LOKALNE sąsiedztwo planszy, nie całość — oszczędza tokeny
  i redukuje szum.

## Budżet

Praktycznie darmowe — jeden raport dosłownie loguje $0.000000. Jeśli wychodzi więcej
niż grosze, podejście jest przekombinowane względem tego co zadanie wymaga.

Pełny kontekst: `source/tool-inventory.md` (sekcja 2.1), `source/community-intel.md`
(sekcja s03e03).
